# TrafficLight-Realm
This project simulates a traffic light control system using Simulink. A clock-driven logic cycles through three states—Red, Yellow, and Green—using modulo and multiport switch blocks
